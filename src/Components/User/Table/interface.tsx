export interface Values {
    userList: {
        id: string;
        username: string,
        firstname: string,
        lastname: string,
        usertype: string,
        usergroup: string,
        resetpassword: string,
    }[];
}